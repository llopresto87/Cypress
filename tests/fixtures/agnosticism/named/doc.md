# A page that never made it through the gate

This procedure was written for widgetco and still says so.

It installs into /srv/widgetco/bin and its steps are owned by j.doe.

The WidgetCo-Gateway component is named outright, in another casing.

Nothing above has been generalized yet.

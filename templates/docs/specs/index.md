# Specs index

| ID | Title | Status | Owner | Last reviewed |
|---|---|---|---|---|

<!--
Status column mirrors each spec's frontmatter `status:` (single home is the
spec; this table is a rendered view). Values: draft | active | implemented |
back-written | open | deferred | hotfix | rejected | superseded | closed.
Regenerate this table from `python3 docs/graph/status-register.py --by-kind spec`
rather than editing it by hand. Add a row when a new spec is created here.
-->

# Sources/normalized

Clean-Markdown summaries of external sources. One file per source,
matching the slug used in `raw/` and `../index.md`. Each carries a
metadata block whose `raw:` line names its snapshot in `raw/`, or why
none was kept (`skill.research-and-ingest` §3).


# API references

Public and internal API references generated from or maintained
alongside the code. Each API has:
- A specification (in `../specs/` if it's user-facing behavior).
- A reference file here describing the endpoints, methods, and
  contracts.
- A wiki page in `../libraries/` if it depends on an external
  framework.

